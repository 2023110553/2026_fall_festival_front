import styled from 'styled-components'

const Wrapper = styled.section`
  margin: 0 16px 16px;
`

// "지금 공연 중" 카드 스와이프(최대 3개 미리보기) + 전체 일정 보기 → PerformancePage로 이동
export default function NowPlayingCards() {
  return <Wrapper>NowPlayingCards placeholder</Wrapper>
}
